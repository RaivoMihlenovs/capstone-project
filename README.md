# To setup locally:
In the terminal enter: npm install, cd client, npm install

# Setup a postgresql database:
In the .env file replace the DATABASE_URL placeholder with your database URL

# To run after setup:
In the terminal enter: npm run dev, cd client, npm start
